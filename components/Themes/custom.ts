import { system } from 'styled-system';

export const perspective = system({
  perspective: {
    property: 'perspective',
    scale: 'perspectives',
  },
});

export const transform = system({
  transform: {
    property: 'transform',
    scale: 'transforms',
  },
  transformOrigin: {
    property: 'transformOrigin',
    scale: 'transformOrigins',
  },
});

export const transition = system({
  transition: {
    property: 'transition',
    scale: 'transitions',
  },
});
